diff --git a/flutter_legado/lib/src/widgets/search_filter_panel.dart b/flutter_legado/lib/src/widgets/search_filter_panel.dart
index 56a4b6369..0179f79b7 100644
--- a/flutter_legado/lib/src/widgets/search_filter_panel.dart
+++ b/flutter_legado/lib/src/widgets/search_filter_panel.dart
@@ -5,9 +5,11 @@ import '../models/models.dart';
 import '../providers/search_provider.dart';
 import '../services/rust_api.dart';
 
-/// 鎼滅储婧愮瓫閫夐潰鏉?-/// 
-/// 浠ュ簳閮ㄥ脊鍑洪潰鏉垮舰寮忓睍绀烘墍鏈夊彲鐢ㄧ殑涔︽簮锛屾敮鎸佸閫?+/// 鎼滅储鑼冨洿绛涢€夐潰鏉?+///
+/// 瀵归綈瀹夊崜绔?SearchScopeDialog锛屾敮鎸佷袱绉嶇瓫閫夋ā寮忥細
+/// - 鍒嗙粍妯″紡锛氭寜涔︽簮鍒嗙粍澶氶€夛紙Checkbox锛?+/// - 涔︽簮妯″紡锛氭寜鍗曚釜涔︽簮澶氶€夛紙Checkbox锛? class SearchFilterPanel extends StatefulWidget {
   const SearchFilterPanel({super.key});
 
@@ -30,22 +32,33 @@ class SearchFilterPanel extends StatefulWidget {
   }
 }
 
-class _SearchFilterPanelState extends State<SearchFilterPanel> {
+class _SearchFilterPanelState extends State<SearchFilterPanel>
+    with SingleTickerProviderStateMixin {
   List<BookSource> _sources = [];
   bool _loading = true;
   String? _error;
   String _searchQuery = '';
   final TextEditingController _searchController = TextEditingController();
+  late final TabController _tabController;
+
+  /// 浠庝功婧愬垪琛ㄤ腑鎻愬彇鐨勬墍鏈夊垎缁勫悕
+  List<String> _groups = [];
 
   @override
   void initState() {
     super.initState();
+    _tabController = TabController(length: 2, vsync: this);
+    // Tab 鍒囨崲鍚庡埛鏂扮粺璁¤涓庡叏閫夋寜閽姸鎬?+    _tabController.addListener(() {
+      if (!_tabController.indexIsChanging) setState(() {});
+    });
     _loadSources();
   }
 
   @override
   void dispose() {
     _searchController.dispose();
+    _tabController.dispose();
     super.dispose();
   }
 
@@ -56,6 +69,7 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
       if (mounted) {
         setState(() {
           _sources = sources;
+          _groups = _extractGroups(sources);
           _loading = false;
         });
       }
@@ -69,6 +83,24 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
     }
   }
 
+  /// 浠庝功婧愬垪琛ㄤ腑鎻愬彇鎵€鏈変笉閲嶅鐨勫垎缁勫悕
+  List<String> _extractGroups(List<BookSource> sources) {
+    final groupSet = <String>{};
+    for (final source in sources) {
+      final group = source.bookSourceGroup;
+      if (group != null && group.isNotEmpty) {
+        // 涔︽簮鍒嗙粍鍙兘鍖呭惈澶氫釜缁勫悕锛堥€楀彿鍒嗛殧锛?+        final parts = group.split(RegExp(r'[,锛宂')).map((g) => g.trim());
+        for (final g in parts) {
+          if (g.isNotEmpty) groupSet.add(g);
+        }
+      }
+    }
+    final list = groupSet.toList()..sort();
+    return list;
+  }
+
+  /// 鎸夋悳绱㈠叧閿瓧杩囨护鍚庣殑涔︽簮鍒楄〃
   List<BookSource> get _filteredSources {
     if (_searchQuery.isEmpty) return _sources;
     return _sources
@@ -78,10 +110,17 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
         .toList();
   }
 
+  /// 鎸夋悳绱㈠叧閿瓧杩囨护鍚庣殑鍒嗙粍鍒楄〃
+  List<String> get _filteredGroups {
+    if (_searchQuery.isEmpty) return _groups;
+    return _groups
+        .where((g) => g.toLowerCase().contains(_searchQuery.toLowerCase()))
+        .toList();
+  }
+
   @override
   Widget build(BuildContext context) {
     final provider = context.watch<SearchProvider>();
-    final selectedUrls = provider.selectedSourceUrls;
 
     return SafeArea(
       top: false,
@@ -102,27 +141,36 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
               ),
             ),
             const SizedBox(height: 12),
-            // 鏍囬
+            // 鏍囬琛?             Row(
               children: [
-                Text('閫夋嫨鎼滅储婧?, style: Theme.of(context).textTheme.titleMedium),
+                Text('鎼滅储鑼冨洿', style: Theme.of(context).textTheme.titleMedium),
                 const Spacer(),
-                if (selectedUrls.isNotEmpty)
+                if (provider.hasFilter)
                   TextButton(
                     onPressed: () {
-                      provider.clearSourceFilter();
+                      provider.clearAllFilter();
                       Navigator.pop(context);
                     },
                     child: const Text('娓呴櫎鍏ㄩ儴'),
                   ),
               ],
             ),
+            const SizedBox(height: 4),
+            // Tab 鍒囨崲锛氬垎缁?/ 涔︽簮
+            TabBar(
+              controller: _tabController,
+              tabs: const [
+                Tab(text: '鍒嗙粍'),
+                Tab(text: '涔︽簮'),
+              ],
+            ),
             const SizedBox(height: 8),
             // 鎼滅储妗?             TextField(
               controller: _searchController,
               decoration: InputDecoration(
-                hintText: '鎼滅储涔︽簮...',
+                hintText: '鎼滅储...',
                 prefixIcon: const Icon(Icons.search, size: 20),
                 suffixIcon: _searchQuery.isNotEmpty
                     ? IconButton(
@@ -143,100 +191,21 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
             ),
             const SizedBox(height: 8),
             // 缁熻淇℃伅
-            Row(
-              children: [
-                Text(
-                  '宸查€夋嫨 ${selectedUrls.length} / ${_sources.length} 涓功婧?,
-                  style: Theme.of(context).textTheme.bodySmall,
-                ),
-                const Spacer(),
-                TextButton(
-                  onPressed: () {
-                    // 鍏ㄩ€?鍙栨秷鍏ㄩ€?-                    if (selectedUrls.length == _sources.length) {
-                      provider.clearSourceFilter();
-                    } else {
-                      for (final source in _sources) {
-                        if (!selectedUrls.contains(source.bookSourceUrl)) {
-                          provider.toggleSource(source.bookSourceUrl);
-                        }
-                      }
-                    }
-                  },
-                  child: Text(selectedUrls.length == _sources.length ? '鍙栨秷鍏ㄩ€? : '鍏ㄩ€?),
-                ),
-              ],
-            ),
+            _buildStatsRow(provider),
             const Divider(),
-            // 涔︽簮鍒楄〃
+            // 鍐呭鍖哄煙
             Expanded(
               child: _loading
                   ? const Center(child: CircularProgressIndicator())
                   : _error != null
-                      ? Center(
-                          child: Column(
-                            mainAxisSize: MainAxisSize.min,
-                            children: [
-                              Icon(Icons.error_outline,
-                                  size: 48, color: Theme.of(context).colorScheme.error),
-                              const SizedBox(height: 8),
-                              Text('鍔犺浇澶辫触',
-                                  style: Theme.of(context).textTheme.bodyMedium),
-                              const SizedBox(height: 8),
-                              TextButton(
-                                onPressed: _loadSources,
-                                child: const Text('閲嶈瘯'),
-                              ),
-                            ],
-                          ),
-                        )
-                      : _filteredSources.isEmpty
-                          ? Center(
-                              child: Column(
-                                mainAxisSize: MainAxisSize.min,
-                                children: [
-                                  Icon(Icons.search_off,
-                                      size: 48, color: Theme.of(context).colorScheme.onSurfaceVariant),
-                                  const SizedBox(height: 8),
-                                  Text('鏈壘鍒板尮閰嶇殑涔︽簮',
-                                      style: Theme.of(context).textTheme.bodyMedium),
-                                ],
-                              ),
-                            )
-                          : ListView.builder(
-                              itemCount: _filteredSources.length,
-                              itemBuilder: (context, index) {
-                                final source = _filteredSources[index];
-                                final isSelected = selectedUrls.contains(source.bookSourceUrl);
-                                return CheckboxListTile(
-                                  dense: true,
-                                  contentPadding: EdgeInsets.zero,
-                                  value: isSelected,
-                                  onChanged: (value) {
-                                    provider.toggleSource(source.bookSourceUrl);
-                                  },
-                                  title: Text(
-                                    source.bookSourceName,
-                                    maxLines: 1,
-                                    overflow: TextOverflow.ellipsis,
-                                  ),
-                                  subtitle: Text(
-                                    source.bookSourceUrl,
-                                    maxLines: 1,
-                                    overflow: TextOverflow.ellipsis,
-                                    style: Theme.of(context).textTheme.bodySmall?.copyWith(
-                                          color: Theme.of(context).colorScheme.onSurfaceVariant,
-                                        ),
-                                  ),
-                                  secondary: Icon(
-                                    isSelected ? Icons.check_circle : Icons.circle_outlined,
-                                    color: isSelected
-                                        ? Theme.of(context).colorScheme.primary
-                                        : Theme.of(context).colorScheme.onSurfaceVariant,
-                                  ),
-                                );
-                              },
-                            ),
+                      ? _buildErrorView()
+                      : TabBarView(
+                          controller: _tabController,
+                          children: [
+                            _buildGroupList(provider),
+                            _buildSourceList(provider),
+                          ],
+                        ),
             ),
             const SizedBox(height: 8),
             // 纭畾鎸夐挳
@@ -244,7 +213,7 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
               width: double.infinity,
               child: FilledButton(
                 onPressed: () => Navigator.pop(context),
-                child: Text('纭畾 (${selectedUrls.length})'),
+                child: const Text('纭畾'),
               ),
             ),
           ],
@@ -252,4 +221,201 @@ class _SearchFilterPanelState extends State<SearchFilterPanel> {
       ),
     );
   }
+
+  /// 缁熻淇℃伅琛?+  Widget _buildStatsRow(SearchProvider provider) {
+    final isGroupTab = _tabController.index == 0;
+    String info;
+    if (isGroupTab) {
+      info = '宸查€夋嫨 ${provider.selectedGroups.length} / ${_groups.length} 涓垎缁?;
+    } else {
+      info = '宸查€夋嫨 ${provider.selectedSourceUrls.length} / ${_sources.length} 涓功婧?;
+    }
+
+    return Row(
+      children: [
+        Text(info, style: Theme.of(context).textTheme.bodySmall),
+        const Spacer(),
+        TextButton(
+          onPressed: () {
+            if (isGroupTab) {
+              // 鍏ㄩ€?鍙栨秷鍏ㄩ儴鍒嗙粍
+              if (provider.selectedGroups.length == _groups.length) {
+                provider.clearGroupFilter();
+              } else {
+                for (final group in _groups) {
+                  if (!provider.selectedGroups.contains(group)) {
+                    provider.toggleGroup(group);
+                  }
+                }
+              }
+            } else {
+              // 鍏ㄩ€?鍙栨秷鍏ㄩ儴涔︽簮
+              if (provider.selectedSourceUrls.length == _sources.length) {
+                provider.clearSourceFilter();
+              } else {
+                for (final source in _sources) {
+                  if (!provider.selectedSourceUrls.contains(source.bookSourceUrl)) {
+                    provider.toggleSource(source.bookSourceUrl);
+                  }
+                }
+              }
+            }
+          },
+          child: Text(_isAllSelected(provider, isGroupTab) ? '鍙栨秷鍏ㄩ€? : '鍏ㄩ€?),
+        ),
+      ],
+    );
+  }
+
+  bool _isAllSelected(SearchProvider provider, bool isGroupTab) {
+    if (isGroupTab) {
+      return _groups.isNotEmpty && provider.selectedGroups.length == _groups.length;
+    } else {
+      return _sources.isNotEmpty && provider.selectedSourceUrls.length == _sources.length;
+    }
+  }
+
+  /// 閿欒瑙嗗浘
+  Widget _buildErrorView() {
+    return Center(
+      child: Column(
+        mainAxisSize: MainAxisSize.min,
+        children: [
+          Icon(Icons.error_outline,
+              size: 48, color: Theme.of(context).colorScheme.error),
+          const SizedBox(height: 8),
+          Text('鍔犺浇澶辫触', style: Theme.of(context).textTheme.bodyMedium),
+          const SizedBox(height: 8),
+          TextButton(
+            onPressed: _loadSources,
+            child: const Text('閲嶈瘯'),
+          ),
+        ],
+      ),
+    );
+  }
+
+  /// 鍒嗙粍鍒楄〃锛堝榻愬畨鍗撶 SearchScopeDialog 鐨勫垎缁勬ā寮忥級
+  Widget _buildGroupList(SearchProvider provider) {
+    final filteredGroups = _filteredGroups;
+    if (filteredGroups.isEmpty) {
+      return Center(
+        child: Column(
+          mainAxisSize: MainAxisSize.min,
+          children: [
+            Icon(Icons.folder_off,
+                size: 48, color: Theme.of(context).colorScheme.onSurfaceVariant),
+            const SizedBox(height: 8),
+            Text(
+              _groups.isEmpty ? '鏆傛棤涔︽簮鍒嗙粍' : '鏈壘鍒板尮閰嶇殑鍒嗙粍',
+              style: Theme.of(context).textTheme.bodyMedium,
+            ),
+          ],
+        ),
+      );
+    }
+
+    return ListView.builder(
+      itemCount: filteredGroups.length,
+      itemBuilder: (context, index) {
+        final group = filteredGroups[index];
+        final isSelected = provider.selectedGroups.contains(group);
+        // 璁＄畻璇ュ垎缁勪笅鐨勪功婧愭暟閲?+        final count = _countSourcesInGroup(group);
+        return CheckboxListTile(
+          dense: true,
+          contentPadding: EdgeInsets.zero,
+          value: isSelected,
+          onChanged: (value) {
+            provider.toggleGroup(group);
+          },
+          title: Text(
+            group,
+            maxLines: 1,
+            overflow: TextOverflow.ellipsis,
+          ),
+          subtitle: Text(
+            '$count 涓功婧?,
+            style: Theme.of(context).textTheme.bodySmall?.copyWith(
+                  color: Theme.of(context).colorScheme.onSurfaceVariant,
+                ),
+          ),
+          secondary: Icon(
+            isSelected ? Icons.check_circle : Icons.circle_outlined,
+            color: isSelected
+                ? Theme.of(context).colorScheme.primary
+                : Theme.of(context).colorScheme.onSurfaceVariant,
+          ),
+        );
+      },
+    );
+  }
+
+  /// 璁＄畻鏌愪釜鍒嗙粍涓嬬殑涔︽簮鏁伴噺
+  int _countSourcesInGroup(String group) {
+    int count = 0;
+    for (final source in _sources) {
+      final sourceGroup = source.bookSourceGroup;
+      if (sourceGroup != null && sourceGroup.isNotEmpty) {
+        final parts = sourceGroup.split(RegExp(r'[,锛宂')).map((g) => g.trim());
+        if (parts.contains(group)) count++;
+      }
+    }
+    return count;
+  }
+
+  /// 涔︽簮鍒楄〃锛堝榻愬畨鍗撶 SearchScopeDialog 鐨勪功婧愭ā寮忥級
+  Widget _buildSourceList(SearchProvider provider) {
+    final filteredSources = _filteredSources;
+    if (filteredSources.isEmpty) {
+      return Center(
+        child: Column(
+          mainAxisSize: MainAxisSize.min,
+          children: [
+            Icon(Icons.search_off,
+                size: 48, color: Theme.of(context).colorScheme.onSurfaceVariant),
+            const SizedBox(height: 8),
+            Text('鏈壘鍒板尮閰嶇殑涔︽簮',
+                style: Theme.of(context).textTheme.bodyMedium),
+          ],
+        ),
+      );
+    }
+
+    return ListView.builder(
+      itemCount: filteredSources.length,
+      itemBuilder: (context, index) {
+        final source = filteredSources[index];
+        final isSelected = provider.selectedSourceUrls.contains(source.bookSourceUrl);
+        return CheckboxListTile(
+          dense: true,
+          contentPadding: EdgeInsets.zero,
+          value: isSelected,
+          onChanged: (value) {
+            provider.toggleSource(source.bookSourceUrl);
+          },
+          title: Text(
+            source.bookSourceName,
+            maxLines: 1,
+            overflow: TextOverflow.ellipsis,
+          ),
+          subtitle: Text(
+            source.bookSourceUrl,
+            maxLines: 1,
+            overflow: TextOverflow.ellipsis,
+            style: Theme.of(context).textTheme.bodySmall?.copyWith(
+                  color: Theme.of(context).colorScheme.onSurfaceVariant,
+                ),
+          ),
+          secondary: Icon(
+            isSelected ? Icons.check_circle : Icons.circle_outlined,
+            color: isSelected
+                ? Theme.of(context).colorScheme.primary
+                : Theme.of(context).colorScheme.onSurfaceVariant,
+          ),
+        );
+      },
+    );
+  }
 }

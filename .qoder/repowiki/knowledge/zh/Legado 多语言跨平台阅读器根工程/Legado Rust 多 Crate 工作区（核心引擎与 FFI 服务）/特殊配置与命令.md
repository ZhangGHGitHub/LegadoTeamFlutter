需安装 rustup stable、flutter_rust_bridge_codegen、Android NDK（路径在 .cargo/config.toml 中硬编码）。常用命令：`cargo check --workspace`、`cargo test --workspace`、`cargo test -p legado-js --features quickjs`、`cargo clippy --workspace --all-targets -- -D warnings`、`./scripts/build-android.sh release` 或 `./scripts/build-android.ps1 -Mode release`。QuickJS 首次编译耗时约 30 秒。
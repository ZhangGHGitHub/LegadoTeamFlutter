构建前需先运行 `make gen` 执行 `flutter_rust_bridge_codegen generate` 生成 Dart 桥接代码；完整构建 APK 需先编译 Rust 动态库 `cd ../rust && bash scripts/build-android.sh release`，再 `flutter build apk`；Debug 模式使用 `make build-debug`；测试通过 `make test` 同时运行 Rust 和 Flutter 测试；静态检查使用 `make check`。
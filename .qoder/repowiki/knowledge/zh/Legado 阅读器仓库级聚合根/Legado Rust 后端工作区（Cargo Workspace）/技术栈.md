Rust 1.97.1（rust-toolchain.toml 固定版本），Cargo workspace resolver 2；HTTP 客户端 reqwest，数据库 rusqlite（bundled），JS 引擎 rquickjs（可选 feature），Web 框架 axum + tokio，序列化 serde/serde_json，错误处理 thiserror，加密 aes/cbc/ecb/des/base64，正则 regex，XPath sxd-xpath，HTML scraper。Flutter 通过 flutter_rust_bridge_codegen 2.12.0 生成 FFI 绑定。
构建需 Java 17 Toolchain；Room schema 目录位于 `app/schemas/io.legado.app.data.AppDatabase/`，由 KSP 参数 `room.schemaLocation` 控制；release 包通过 `RELEASE_STORE_FILE` 等签名参数签名并启用 R8 混淆；可通过 `armOnly=true` 属性仅打包 arm64-v8a/armeabi-v7a ABI；Cronet 原生库以 JAR/AAR 形式存放在 `cronetlib/` 下直接 fileTree 引用。
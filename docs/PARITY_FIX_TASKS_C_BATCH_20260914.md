# 阶段C 修复任务书（C1/C2/C3）

> 依据：SCREEN_1TO1_PARITY_LEDGER_20260914.md「差异总表与修复规划」；视觉基准 `docs/parity_shots/ref_20260914/`。
> 执行纪律：串行派发（单设备构建/安装冲突）；每批独立版本号+双更新日志+设备验证；commit 不 push（主代理审核后统一推）。
> 设备：MuMu Test 实例 `192.168.1.19:5555`（adb=`D:/leidian/LDPlayer9/adb.exe`；device not found 先 connect；禁用 android-emulator MCP 插件工具）。

## C1 搜索域 + 详情页（版本 2.0.260+261 → 2.0.261+262）

### A. 搜索结果页（1-7），基准 ref 07_search_results.png
- S2【P1】空数据渲染「NaN : NaN」：副标题与 chips 在无作者/最新章节/字数时改为不渲染该行/标签；
- S1 移除结果胶囊下「搜索: N」重复行（保留「结果 N · 进度 x/y」胶囊）；
- S3 移除「暂无专辑」「暂无简介」占位（无数据不渲染）；
- S4 结果项右侧来源名徽标 → **来源数数字角标**（参考 143/13/8）；
- S5 封面音频播放钮+右下播放浮钮：docs/ 与 git 历史检索授权；无证据按红线移除并 CHANGELOG 明示。

### B. 搜索内容页（1-13），基准 ref 13_search_content.png
- C1 标题「搜索正文」→「搜索内容」；
- C2 补「仅本书」深色胶囊（复用既有范围三档逻辑，默认选中样式对齐参考：深色实底）；
- C3 输入 hint →「搜索...」。

### C. 书籍详情页（1-8），基准 ref 08_book_info.png
- D2【P1】动作区改 4 图标卡一行（已在书架/查看目录/书源/阅读记录；设置分组并入次级入口并汇报）；
- D1 封面放大至参考比例（约占宽 1/3、高约 260dp，自截图量化）；
- D3 流派标签云：授权检索，无证据按红线移除；右下「继续阅读」→ 绿色圆形「阅读」FAB；
- D4 章节信息行对齐「共 N 章｜未读/已读」。

**验证**：analyze 0；test 全过；release 装机截图 `docs/parity_shots/ours_2.0.261/{07_search_results,13_search_content,08_book_info}.png`（收紧断言防错态）；台账 1-7/1-13/1-8 行更新；CHANGELOG+updateLog；fix(ui) commit 不 push。

## C2 阅读器域（版本按 C1 后顺延 +1）

### A. 正文（1-10），基准 ref_20260914/10_reader、10b
- R1【P1】默认字号对齐参考（参考约 20-22sp/行 ~14 字；量化参考截图后改默认档）；
- R2 默认行距对齐（参考约 1.7）；
- R4 正文顶部「时间+章节名+进度」状态行：核实头部信息开关，默认关闭（参考无）；
- R5 底栏左侧改显示书名（参考「斗罗大陆」，我方现为章节名截断）。

### B. 阅读器菜单（1-11），基准 ref_20260914/11_reader_menu
- M1【P1】菜单形态收敛为紧凑浮层（参考：顶栏章节名+⋮ 一行、中部小图标、底部功能行，正文大部分可见；我方现为整页大面板）；
- M2 移除来源 URL 全文行（来源名并入小字行）；
- M3 中部圆钮去圆底或对齐参考小图标形态（保留全部功能项）；
- M4 进度形态对齐（参考底行「1/12 + 目录%」，保留滑条能力）；
- M5 亮度条位置核实（参考该态未见）；
- ✅ 底部功能行五项（章节梗概/AI改写/全文搜索/自动翻页/目录）已是同名同序，勿动。

### C. 目录（1-9），基准 ref 09_toc
- T1 tabs 数：ref 2 项（目录/书签）vs 我方 3（多「标注」）——核实后对齐或登记保留；
- T2 章节行右侧：ref 字数胶囊 vs 我方云朵缓存图标 —— 改为字数胶囊（缓存状态移至别处或省略并汇报）；
- T3 标题区：参考大标题（章节名+页码）与 🔍/⋮ 分离，我方行内联 → 对齐；
- T4 移除我方目录底栏「页码+↑↓」条（参考无）；
- T5 当前章高亮底样式对齐（参考淡蓝底）。

**验证**：同 C1 规程；截图 `docs/parity_shots/ours_<本批版本>/{10_reader,11_reader_menu,09_toc}.png`。

## C3 书架与全局图标（版本顺延 +1）

- B1 书架分组 tab 单组时左对齐（参考 tab 左起）；
- B2 溢出菜单改「每项图标+分体圆角卡」形态（基准 ref 03，功能项集不动）；
- B3 文案微差：「书架布局」→「布局设置」、「添加远程书籍」→「远程书籍」；
- B5 底栏订阅图标选形（参考 RSS 图标，我方文档图标）改齐；顶栏钮圆底/裸图标按屏对齐参考（书架/首页=裸图标，搜索=圆底）。
- 配色类（底栏胶囊色/输入条底色/正文背景色）**不在本批**，统一留批 3 主题域。

**验证**：同规程；截图 `ours_<版本>/03_bookshelf.png` + `04_bookshelf_overflow_menu.png`。

编写者：Qoder ｜ 2026-09-14

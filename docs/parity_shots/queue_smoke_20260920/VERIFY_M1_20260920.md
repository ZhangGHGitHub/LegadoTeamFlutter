# VERIFY_M1_20260920 — 队列② M1 + 崩溃误报 设备验证收尾

证据目录：`docs/parity_shots/queue_smoke_20260920/`（截图前缀 `m1c_` / `verify2_`）
判定口径：证据优先；视觉类结论一律来自可复现的 PIL 像素采样（本模型图像输入被省略，无“目视”能力），
凡无法插桩/观测之处显式标注“上限”，不虚构截图/布局/错误检查结果。

---

## 0. 前置：设备与版本

| 项 | 值 |
|---|---|
| 设备 | MuMu Test `192.168.1.19:5555`（SM-G9900 / Android 15，分辨率 1080x1920） |
| adb | `D:/Android/platform-tools/adb.exe` |
| 目标包 | `io.legado.flutter_legado`（机上 2.0.300 / versionCode 301，即 v2.0.3） |
| 启动器 | `io.legado.flutter.MainActivity` |
| root | adbd root（`adb -s S root` → `id`=uid=0(root)）；重启后丢 root，需重新 root |
| 源码/版本差 | 工作区源码为 v2.0.4，机上为 v2.0.3；机上阅读器底部 5 个功能按钮（源码 4 个）+ 独立源操作行，**按钮身份按位置不可靠，须以打开的面板实证识别** |

不触碰设备：`127.0.0.1:16384`（Test2，他任务）、`emulator-5554`（弃用雷电档）。
约束：禁止 commit；不改仓库源码（只落证据）。

ROM/工具绕行（沿用）：`search_keywords` 注入或历史 chip 输入中文；`screencap` 走 `exec-out`；
uiautomator dump 全应用 canvas 渲染、零文本/零内容描述；`/sdcard` 幻影映射 → 用 `/data/local/tmp`；
Git Bash 下 adb `/data/...` 路径前必须 `export MSYS_NO_PATHCONV=1`；`grep` 被 alias 成 ugrep → 解析改用 Python。

---

## 1. M1-a 深色正文背景（纯黑验证）

- 命令：切深色主题后，阅读器正文空区（避开文字）采样。
- 证据：`m1c_03_reader.png`（深色）/`m1c_06_blankband_ours_vs_ref.png`。
- 采样值：正文空区背景 RGB = **(0,0,0)**，n=221,760（避开文字像素的空带）。
- 与参考 Δ：参考纯黑 (0,0,0)，Δlum = **0**（完全一致）。
- 判定：**通过**（深色主题正文背景为纯黑，与参考一致，无灰阶偏差）。

## 2. M1-b 阅读中切主题即时生效（两态各采样）

- 命令：阅读页 → 主题模式 sheet → 切深色 / 切浅色，各采样正文背景。
- 证据：`m1c_26_reader_dark_resample.png`（深色）/ `m1c_23_reader_light.png`（浅色）。
- 采样值：深色正文背景 **(0,0,0)** n=283,680；浅色正文背景 **(254,254,254)** n=282,240。
- 判定：**通过**（切换即时生效，两态背景各自收敛到纯黑/近白，无残留中间态）。

## 3. M1-c 显式选择保留（显式选背景→切主题不覆盖）

- 命令：显式选“绿色”背景 → 再切主题（→浅色）→ 重进阅读器，验证显式选择是否被主题覆盖。
- 证据：`m1c_30_reader_green_bg.png`（显式绿色）/ `m1c_32_reader_light_theme_green_bg.png`（切浅色后重进）。
- 采样值：显式绿色背景 RGB = **(203,234,203)** [m1c_30]；切主题→浅色后重进阅读器仍为 **(203,234,203)** [m1c_32]。
- 判定：**通过**（显式背景选择被保留，未被主题切换覆盖）。

## 4. M1-d chrome（顶/底栏）可读性

- 命令：深色主题下对阅读器 chrome（顶栏/底栏）做对比度采样。
- 证据：`m1c_27_reader_dark_chrome2.png`。
- 采样值：正文 (0,0,0) 116,409px 纯黑；底栏表面 (20,19,19)；chrome 文字为浅灰。
- WCAG 对比度（chrome 文字 vs 底栏表面）：**13.24:1 / 11.76:1 / 10.34:1**（均 ≥ AA 4.5:1，且 ≥ AAA 7:1）。
- 判定：**通过（可读）**。
- 上限声明：本模型图像输入被省略，以上为 PIL 像素对比度计算，**无法目视**确认实际观感；结论限于“对比度数值满足 WCAG 可读阈值”。

---

## 5. 崩溃误报复核

- 命令：查崩溃日志目录 / 崩溃标记 / 冷启动弹窗。
- 证据：`verify2_cs1.png`、`verify2_cs2.png`（两次冷启动）。
- 原始观测：
  - 日志目录 = `getApplicationDocumentsDirectory()` = `/data/user/0/io.legado.flutter_legado/documents/`；该目录**不存在**
    → 无 `crash_log.txt`（无 `===== 崩溃日志 =====` 段）、无 `app_log.txt`（无 `[布局告警]` 段）。
  - SharedPreferences `crash_last_crash` 标记**不存在**（`settings get global` 亦无）。
  - 两次冷启动均**未出现**「上次运行发生崩溃」弹窗。
  - 重启后复核：`crash_last_crash` 仍不存在。
  - 设备 10:27 曾发生**重启**（非崩溃）：重启使 app 进程被系统终止，非应用崩溃。
- 判定：**通过（崩溃为误报）**——无任何崩溃日志/崩溃标记/崩溃弹窗证据；唯一的中断来自设备重启，属环境事件而非应用崩溃。

---

## 6. 回归三探针

### 6.1 正文（通过）
- 证据：`verify2_p3_toc1.png`（正文基线）、`verify2_r2`（重启后复验）。
- 采样值：正文背景 (255,255,255) 264,843px + 正文文字 (51,51,51) 19,633px。
- 判定：**通过**（浅色正文白底黑字正常渲染，重启后复验一致）。

### 6.2 目录（通过）
- 识别：5 个功能按钮逐一实证（以打开的面板定性，不按源码坐标推断）。
  - btn1/btn2（x≈110/330）→ 底部 sheet（scrim 112 + sheet 238）：居中标题 + 左搜索框 + 缩略封面带 + 少量文字，**非章节列表**。
  - btn3/btn4（x≈530/760）→ 全屏 (245) 页：仅数行文字、不可滚动，短设置/选项页。
  - **btn5（x≈970）→ 全屏可滚动章节列表（目录 / TocScreen）**：约 15+ 行（x28-640），可滚动。
- 命令/证据：`verify2_btn5.png`（打开目录）；`verify2_toc_scroll0/1.png`（滚动，内容 md5 d0d87ce2501a→6e211f678156，证明列表可滚动）；`verify2_toc_tap.png` + `verify2_toc_stab1/2.png`（点章节行 (300,1200)）。
- 采样值/判定链：
  - 目录前阅读器正文 md5 基线 = `35fcb8429267`（白底 255）。
  - 点目录章节行后：回到阅读器（白 65278，顶栏白 255），正文 md5 → **`26275aad80a3`**（与基线不同）。
  - 新页为真实章节：白 116159px + 正文文字 12443px；两帧稳定（md5 相同）。
- 判定：**通过**（目录可打开、可滚动；点章节行真实跳转，正文内容 md5 改变且新页有正文；列表可滚动）。

### 6.3 搜索（通过）
- 识别：阅读器底部 FAB 行最左 = 全文搜索 pill（x≈90，y≈1525）。
- 命令/证据：`verify2_search_open.png`（点 pill 打开搜索面板：顶部输入框 + 右搜索按钮 + 键盘）；`verify2_search_input.png`（聚焦输入框并 `input text "的"` 注入中文，输入区 367 深色像素=已入字）；`verify2_search_results.png` / `verify2_search_results2.png`（`KEYCODE_ENTER` 执行搜索，两次稳定）。
- 采样值：搜索执行后出现 **~10 行** 全宽结果行（x64-1012，各行 n=979~2798，均匀分布 y300-1240）；两帧稳定（md5 `62ff0876c963`）。
- 判定：**通过**（全文搜索面板可打开、可接受中文输入、可执行并返回结果列表）。
- 备注：本 ROM（Android 15）下 `input text` 实测可注入中文（与早期“仅 ASCII”的经验不一致，已实测更正）。

---

## 7. 异常登记

| # | 异常/上限 | 说明 |
|---|---|---|
| a | 图像省略上限 | 本模型图像输入被省略，视觉结论一律以可复现 PIL 像素采样为准，无目视 |
| b | 全应用 canvas 渲染 | uiautomator dump 零文本/零内容描述，无法读取控件文字，面板只能靠像素结构定性 |
| c | XML 离线改主题不生效 | 直接改 XML 的主题不生效，且重启后仍保持（须走 UI 切换） |
| d | grep=ugrep | 该 alias 标志不可靠，解析一律改用 Python |
| e | MSYS_NO_PATHCONV 必需 | Git Bash 下 adb 的 `/data/...` 路径会被 mangle，须先 `export MSYS_NO_PATHCONV=1` |
| f | 启动器 | 我方包启动器为 `io.legado.flutter.MainActivity`（非默认 MainActivity 全名） |
| g | 旧坐标失效 | 重启前旧坐标 (204,1034)/(972,1806)/(135,1616) 已失效——目录点按曾命中滑条带 |
| h | /sdcard 幻影映射 | 用 `/data/local/tmp` + `exec-out cat` 替代 |
| i | uia segfault 噪音 | dump 时有 segfault 噪音，忽略 |
| j | m1c_08 文件名误导 | 承继项，文件名与内容不符 |
| k | MuMu 10:27 重启 | 非崩溃，app 进程被重启终止（环境事件） |
| l | 版本按钮数不一致 | 机上 v2.0.3 有 5 功能按钮 vs 源码 v2.0.4 有 4 按钮，坐标不可照抄源码 |
| m | 重启后丢 root | 需重新 `adb root` |
| n | 连续 BACK 状态漂移 | 旧 btn3-5 截图不可靠，已弃用，以本段 s0 起重验为准 |

---

## 8. git 状态（本任务未 commit、未改源码）

- 本任务仅新增未跟踪证据文件：`docs/parity_shots/queue_smoke_20260920/` 下 `m1c_*` / `verify2_*` 共 100 个 + 本报告。
- `git status --short` 中 `M` 的 9 个文件（`docs/SCREEN_1TO1_PARITY_LEDGER_20260914.md`、
  `flutter_legado/lib/src/screens/settings_home_screen.dart`、`.../settings_screen.dart`、
  `.../widgets/paragraph_layout_engine.dart`、4 个 `test/` 文件）为**在途 A 组既有未提交改动**，非本任务产生；本任务未触碰任何 `.dart` 源码。
- 未执行任何 commit。

# 设备复核轮（Test2）· UI 批次落地实机核验记录

- 日期：2026-09-22
- 设备：Test2 = MuMu 127.0.0.1:16384（逻辑多屏，app 窗口在独立逻辑屏，input 需 `-d <displayId>`，screencap 需 SF displayId；每次 force-stop 后 displayId 会变，当前逻辑屏=7 / SF=4619827052952829958）
- 应用：io.legado.flutter_legado（只操作此包）
- 证据目录：docs/parity_shots/verify_ui_20260922/（前缀 vui_）
- 判据基准：ref_dark_20260921/（04_appearance_clean_dark*）与 ref_dark_20260920/05_theme.png

## 0. 装包与版本前置

- 命令：`pwsh.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\emulator_smoke_test.ps1 -Device 127.0.0.1:16384`
- DECISION：Rust FFI 指纹失配（.so 陈旧）→ 脚本自动重编 aarch64（54.47s）+ x86_64（43.12s）→ FFI verify PASSED → flutter build apk --debug（APK 265.2MB）→ 安装成功
- 版本核对：versionName 2.0.306 / versionCode 307，与 pubspec 一致
- 冒烟结果：7 项 PASS，退出码 0（进程存活 + 无崩溃日志）

## 项1：外观页深色帧 与参考基线并排核对

操作路径：首页 → 底部「我的」tab → 下滑 → 「设置」卡(540,1224) → 「外观」(540,579)；主题模式切「深色」(828,429)。

证据：
- 我方顶帧：vui_03_appear_dark_top.png + vui_appear_dark_top.xml（顶部帧语义）
- 我方中帧：vui_04_appear_dark_mid.png + vui_appear_mid.xml / vui_appear_mid2.xml / vui_appear_mid3.xml / vui_appear_bottom.xml（整页分组链）
- 并排图：vui_05_appear_dark_top_side.png（顶帧）、vui_06_appear_dark_mid_side.png（中帧）
- 参考：ref_dark_20260921/04_appearance_clean_dark.png/.xml（顶帧）、04_appearance_clean_dark_mid.png/.xml（中帧）

说明：模型无图像输入，视觉核验用 uiautomator 语义（text/content-desc + bounds）+ PIL 像素采样；ref 用 text 节点，我方 Flutter 语义在 content-desc。两帧屏幕分辨率不同（ref 1080x1920 vs 我方 1080x2220），像素级 diff 不适用，以结构对比为准。

### 1.1 一致项（通过）

1. 页标题「外观」：双方均有（我方 [216,48][324,120] 常规顶栏；ref [48,264][216,386] 大标题形态）。
2. 「主题模式」分组存在且为页首第一组（我方 [48,168][1032,318]；ref [96,1584][266,1645]）。
3. 分段选择器三选项：跟随系统 / 浅色 / 深色，顺序一致。
4. 色卡行存在，草野 / 柠檬 / 黑白 三色卡双方均有。
5. 「切换图标」组双方均有（ref 副题「切换软件显示在桌面的图标」，我方同文案）。
6. 「字体大小」双方均有（ref 副题「当前字体大小: 1.0」，我方副题「跟随系统」）。
7. 任务点名的 4 组顺序：主界面 → 书籍详情页 → 容器设置 → 模糊效果，我方齐全且顺序正确（mid2/mid3/bottom dump 确认）。

### 1.2 像素采样（深色）

| 采样点 | 我方 | ref |
|---|---|---|
| 页面背景 | #4A4A4A（中性灰，Material 引擎深灰） | #101418（偏蓝深色） |
| 分段·未选中底 | #646464 | #1D2024 |
| 分段·选中（深色） | #D9CFCE | #9ECAFC（浅蓝高亮） |
| 色卡·草野 | #12140E | #101418 |
| 色卡·柠檬 | #15130B | #3B4857 |
| 色卡·黑白 | #141313 | #101418 |
| 色卡·电子书 | #000000 | （ref 无此卡） |

### 1.3 差异点（分类）

需修（若以 ref 基线为对齐目标）：
- D1 色卡行缺「动态取色」卡：ref 色卡行 = 动态取色/草野/柠檬/黑白（4 卡，动态取色居首，[75,675][213,723]）；我方色卡行 = 草野/柠檬/黑白/电子书（无动态取色）。
- D2 缺「预见性返回手势」组（ref 有：启用系统预见性返回手势 [96,1435]）。
- D3 缺「主题管理」组（ref 中帧底部 [96,1879][291,1920] 有组标题；我方主题相关能力散在「自定义主题·白天/夜间」组）。
- D4 缺「字体设置」独立组（ref [96,819] 独立组；我方仅「字体大小」单项且副题为「跟随系统」而非当前值）。

待裁决（结构/设计取向差异，非 bug）：
- D5 我方多出的组：主题引擎、配色风格、纯黑深色模式、启动界面样式、沉浸式状态栏、沉浸式导航栏、导航栏阴影、封面设置、底栏图集、跟随壁纸配色、主界面、书籍详情页、容器设置、模糊效果、自定义主题·白天/夜间 —— ref 均无（ref 外观页仅 8 组：主题模式/色卡行/字体设置/主题风格/切换图标/预见性返回手势/字体大小/主题管理）。我方为重构后扩展，其中 4 组（主界面/书籍详情页/容器设置/模糊效果）为任务已点名确认项，属有意新增。
- D6 分组命名差异：我方「主题引擎 / 配色风格（关闭（内置色板））」vs ref「主题风格（Material Design）」。
- D7 标题形态：ref 大标题（large top app bar，标题 y264 高 122px，返回键在标题上方一行）；我方常规顶栏（标题 y48 与返回键同行）。
- D8 ref 顶帧有中央插画/预览区 [348,456][732,1224]（384x768，主色 #101010/#304050/#90C0F0，疑似主题预览图），我方无。
- D9 深色背景底色：我方 #4A4A4A（Material 引擎中性深灰，composeEngine=material 等价下符合）vs ref #101418（偏蓝深色）。属主题引擎/色板差异，与「配色风格·内置色板」取值相关，需确认基线引擎要求。

可忽略：
- D10 ref 顶帧有提示条「仍有部分界面未用Compose重构…」+「关闭」按钮（[96,1332][840,1453]+[840,1320][984,1464]）：属 ref 历史迁移期通知，我方已全量 Flutter 化，无此通知，非缺陷。
- D11 屏幕分辨率差异（1080x1920 vs 1080x2220）：设备侧差异，不影响代码判定。

## 项2：主题选择页（外观页主题卡行）横滑 64dp 方卡与选中态

操作路径：首页 → 我的 tab →「主题模式」快捷卡(540,280)（打开「选择主题模式」底部 sheet：跟随系统/浅色/深色 + Dismiss(540,1188)，非方卡页，关闭）→ 我的 → 设置(540,1224) → 外观(540,579) → 外观页主题卡行（HorizontalScrollView [60,534][1020,834]，横滑验证）→ 点卡切换主题（千禧年 → 草野复原）。

证据：
- vui_07_theme_cards.png（初位：草野/柠檬/黑白/电子书）+ vui_item2_cards.xml
- vui_08_theme_side.png（与 ref_dark_20260920/05_theme.png 并排）
- vui_09_cards_scrolled.png + vui_item2_cards2.xml（横滑后：春/千禧年/隐海修会/乐队/透明）
- vui_10_cards_switched.png（千禧年选中：#565992 描边 + 中心圆点）
- vui_12_cards_restored2.png（草野选中：#4C662B 四缘描边 + 中心圆点）
- ref：05_theme.png/.xml（ref 卡行=动态取色/草野/柠檬/黑白；选中卡「动态取色」中心 #9ECAFC 圆点 + #514060 边缘色带）

判定：
1. 通过 — 64dp 方卡：卡宽 192px=64dp（@3x），色板区 192x192 方形 + 下方 28dp 名称文案，形态与 ref 一致。
2. 通过 — 横滑：卡行为 HorizontalScrollView，横滑可显更多命名主题（草野/柠檬/黑白/电子书/春/千禧年/隐海修会/乐队/透明/晴空/八月/新浪潮 等 ≥12 张），滑动无卡顿异常。
3. 通过 — 选中态 2dp 主色描边：千禧年选中时四缘 #565992 5-6px（≈2dp@3x）；草野选中时四缘 #4C662B 6-7px（vui_12）。
4. 通过 — 选中态 40dp 主色圆点勾选：千禧年卡中心 #565992 圆点（宽约 110px≈37dp）、草野卡中心 #4C662B 圆点（高约 98px≈33dp），与「40dp 主色圆点勾选」规格接近（±10%）。ref 选中卡同为中心圆点（#9ECAFC）+ 边缘色带，形态一致。

差异点：
- T1（需修）：缺「动态取色」卡（ref 卡行首卡；我方无）。与项1 D1 同源。
- T2（待裁决）：主题列表为扩展集（ref 4 张 vs 我方 ≥12 张，我方多出 电子书/春/千禧年/隐海修会/乐队/透明/晴空/八月/新浪潮 等命名主题）。
- T3（待复核）：内置色板态（配色风格=关闭，页背景 #4A4A4A，vui_07）下草野选中卡的描边/圆点不显著（#F9FAEF 系与卡左半融色）；主题色板态（页背景 #1A1C16，vui_12）下描边/圆点清晰（#4C662B）。选中态视觉显著性依赖配色状态，需确认基线对内置色板态的要求。

状态变更登记（复原情况）：
- 主题：草野 → 千禧年 → 草野（已复原为草野）。
- 配色风格：已复原「关闭（内置色板）」。
- 残留差异：当前主题为「草野」命名主题（外观页背景 #1A1C16）；操作前为内置色板默认（背景 #4A4A4A）。外观页未见「清除命名主题回内置」入口，未做破坏性重置，残留待下轮处理或经裁决接受。

## 项3：搜索输入盒（A6 回退后深色填充色）

**操作路径**：首页 → 底部「发现」tab(540,1800) → 顶部右侧「展开搜索」(882,84) → 截图 vui_15_search_expanded.png + dump vui_item3_searchbox.xml

**原始输出（dump 结构）**：
- 外盒容器 View [48,192][1032,312]（984x120px）
- 输入框 EditText [180,216][960,288]（780x72px，无 content-desc 语义）
- 顶部按钮「收起搜索」[828,30][936,138]、「更多」[960,30][1068,138]

**像素采样（vui_15_search_expanded.png）**：

| 点 | 坐标 | 值 | 说明 |
|---|---|---|---|
| 盒内 | (540,250) (700,270) (400,225) | #1A1C16 | 填充色一致 |
| 盒内局部 | (250,240) | #1F211B | 略亮，疑输入区内图标/文字抗锯齿 |
| 盒内左 | (120,250) | #C5C8BA | 浅色，应为搜索图标（onSurfaceVariant 级） |
| 盒外页背景 | (300,84) (540,345) | #12140E | 当前主题背景 |
| 盒四角 | (60,205)(1020,205)(60,300)(1020,300) | #12140E | 圆角外，落在页背景 |

**判定：通过** —— 深色下搜索输入盒填充 #1A1C16，比页背景 #12140E 亮一级，正是 surfaceContainerLow 层级（当前「草野」命名主题深色下的容器色）；**不是 onSurface 白盒**（若白盒应为 #FAFAFA 级浅色，实测全盒暗色）。A6 回退在实机视觉生效。

**状态备注**：采样时应用处于「草野」命名主题 + 深色（项2 残留状态，见项2 状态变更登记）。该主题 containerLow≈#1A1C16；即使恢复内置色板（Material 引擎深色 #4A4A4A 基），填充仍为深色容器色，判定不变。

## 项5：失败书源非阻断横幅（favcomic）

**操作路径**：书架 tab →「搜索」(900,96) → 点历史项触发全书源搜索（关键词「斗破苍穹斗破苍穹」）→ 等 916 源搜完（「结果 3902 · 进度 916/916」）→ 截图收起态 → 点横幅展开 → 截图+dump

**原始输出（dump 节点）**：
- 统计行：「结果 3902 · 进度 916/916」[307,546][773,603]
- 横幅（收起）：「111 个书源搜索失败，点击展开」[48,621][1032,723]（click 可展开/收起）
- 结果列表在横幅下方继续（非阻断）：「斗破苍穹斗破苍穹 的搜索结果 / biu讯」卡 [48,855][1032,1047] +「加载下一页」[888,1728][1032,1872]
- 横幅展开后 [48,621][1032,1920]，逐条列出失败源「图标+源名+错误类型: 详情」

**失败明细抽样（展开态 dump，共 111 条，节选）**：
- 🎑七猫小说·API：JS engine error: 此书源需要 Java 脚本能力（Packages.java.security.MessageDigest），当前不支持
- 🏆🏮终极全栖接口聚合：Network error: Connection failed (https://cj.rycjapi.com/…)
- 🟣古龙全集：Parser error: 书源未配置 searchUrl
- ⚡🟣77读书 / 🟣台湾小说网 / 🟣文学小说：JS engine error: 搜索结果列表解析失败: not a function (…)
- 🟣霹雳书屋 / 🏮Linpx / 🟣三五文学：Login required: 书源需要登录
- ⚡🟣若雨中文 / 🟣落霞读书 / 🎑拷贝漫画：Network error: Connection failed (…)
- 🎨🎨喜漫漫画：Network error: Connection failed: error sending request for url (https://www.favcomic.com/search?keyword=…) ← **favcomic 源（源名「喜漫漫画」）在失败列表内**
- ⚠️白鹿书院 / 🎑阅宝书屋 / ⚡米读小说：Network error: 搜索超时（30s）
- ⚡笔趣阁#3 / 🏮爱丽丝书屋：Timeout: Request timeout (…)

**判定：通过** —— ① 非阻断：收起态横幅高 102px（[621,723]），下方 3902 条结果列表/「加载下一页」正常可达，搜索流程未被阻断；② 展开后逐条显示源名+错误文案（111 条，错误类型覆盖 JS engine error / Network error / Login required / Parser error / Timeout 五类）；③ 任务点名的 favcomic（喜漫漫画，https://www.favcomic.com）在失败明细中。

**证据**：vui_40_failed_banner_collapsed.png（收起态）、vui_41_failed_banner_expanded.png（展开态）、vui_item4_after2back.xml（收起态 dump）、vui_item5_banner_expanded.xml（展开态 dump，含 111 条明细全文）

**环境备注**：本次搜索 916/916 完成，111 源失败（网络受限环境：大量 Network error/Timeout，属设备网络环境问题，非 UI 缺陷）。

## 项4：阅读器菜单 5 键（全文搜索/自动翻页/目录/朗读/设置）

**操作路径**：书详页「阅读」(887,1788) → 读者 → 点屏中心(540,960)唤菜单 → 截图 vui_39_reader_menu.png + dump vui_item4_readermenu.xml；再次进入截图 vui_46_reader_again.png

**原始输出（dump，漫画阅读器）**：
- 标题：「斗破苍穹斗破苍穹 的搜索结果 / 第 1 / 0 章」[0,0][1080,1920]
- 顶栏：「返回」[0,12][144,156]、「漫画设置」[936,12][1080,156]
- 正文区：「Parser error: 刷新目录失败：未从书源解析到任何章节（书源可能失效，请尝试换源）」[96,861][984,1083] +「重试」[387,1155][693,1299]
- 底栏：「0%」[192,1686][336,1830]

**判定：无法判定（环境受限）** —— 参考要求的 5 键（全文搜索/自动翻页/目录/朗读/设置）是**文字阅读器**菜单；本环境唯一可搜到的书（biu讯/🏮Bi话 漫画源）命中漫画，进入的是**漫画阅读器**（菜单键为 返回/漫画设置/进度 0%，已 dump 为替代证据）。916 源重搜返回「未找到可替换的书源」（唯一命中源解析不出章节），无其他可读文字书；书架仅此 1 书、阅读记录为空。故 5 键文字菜单本轮无法取证，非 UI 缺陷，属测试环境书籍供给问题。

**建议**：下一轮在有外网/可用文字书源的实机环境补验 5 键文字菜单；或预置一本可解析的文字书到设备 DB 后再验。

## 项6：书架单击直达阅读 + 返回刷新进度

**操作路径**：书架 tab → 点书卡(204,962) → 书详页；（试双击：第二次点击落在详页「已在书架」按钮 → 触发「移出书架」确认框，已点「取消」保住书）；详页「阅读」(887,1788) → 读者 → 两次 BACK 回书架

**观察（原始 dump）**：
1. **单击行为**：书架卡 [66,902][342,1022] 单击 → 进**书详页**（dump：编辑书籍信息/分享/更多 + 「已在书架」[39,888][267,1095] +「阅读」按钮 [743,1704][1032,1872]），**不是直达阅读**
2. 双击（0.3s 间隔两次单击）：第一次已进详页，第二次命中详页「已在书架」按钮 →「移出书架」确认框（「确定将《斗破苍穹斗破苍穹 的搜索结果》移出书架吗？」），已取消
3. 返回书架后卡内容：desc 仍仅「斗破苍穹斗破苍穹 的搜索结果」[66,902][342,1022]，**无进度副文案**（如「读到第 X 章/进度 Y%」）

**判定**：
- 单击直达阅读：**差异** —— 实机行为为「单击进书详页，需再点『阅读』才进读者」，与参考基线「书架单击直达阅读（未读书→首章）」不符。归类：**待裁决**（可能是原 Legado 经典交互设计：单击详页/双击移除；若项目基线要求单击直达阅读则为需修项）
- 返回刷新进度：**无法判定** —— 唯一书为漫画源且目录解析失败（「目录：加载目录失败」），无法产生真实阅读进度；返回后书架卡无进度展示可对照。环境限制，非 UI 缺陷。

**证据**：vui_44_shelf_tap_read.png（单击→详页）、vui_45_double_tap.png（移出书架框）、vui_46_reader_again.png（读者）、vui_47_back_shelf.png（回书架）、vui_item6_reader.xml / vui_item6_shelf.xml / vui_item6_back_shelf.xml

## 项7：⑦c 其余点（选择器选中态 / 通用组滚动观感 / 纯黑开关显示）

**操作路径**：我的 tab →「设置」卡(540,1224) →「外观」(540,579) → 截图+采样（vui_52_appear_top.png / vui_item7_appear.xml）→ 滚动两轮（vui_53_appear_scrolled / vui_54_appear_bottom + dump）

### 7.1 主题模式选择器选中态视觉
- 选择器三选项：跟随系统 [117,366][387,492] / 浅色 [405,366][675,492] / 深色 [693,366][963,492]（当前选中「深色」）
- 像素采样：未选项（跟随系统/浅色）填充 #33362E；选中「深色」(828,429) = **#B1D18A**（草野主题主色浅绿填充）
- **判定：通过** —— 选中态用主色浅色填充与未选项暗灰绿（#33362E）建立明度层级，视觉清晰，非仅靠描边/颜色点缀；符合 Apple 风格「用尺寸与明度建立层级、克制 accent」判据。

### 7.2 纯黑开关显示（参考 !miuix 条件，我方无条件显示）
- 行「纯黑深色模式 / 暗色模式背景纯黑（省电护屏）」[60,1248][1020,1500] 在外观页正常显示
- switch 组件采样：track（880/920,1374）= #33362E，thumb（940,1340/1410）= #8F9285 → 当前 **OFF** 态
- **判定：通过（观察）** —— 本机构成引擎为 material（与参考 !miuix 条件等价应显示），开关无条件显示且渲染正常，与参考预期一致。

### 7.3 通用组滚动观感
- 外观页可滚动，两轮上滑：第一屏（主题模式/主题卡行/主题引擎/配色风格/纯黑/切换图标/字体大小 顶部）→ 第二屏（切换图标/字体大小/主题管理/导出·导入主题/启动界面样式/沉浸式状态栏/沉浸式导航栏/导航栏阴影 elevation 0）→ 第三屏（沉浸式导航栏/导航栏阴影/封面设置/底栏图集/跟随壁纸配色（Android 12+/iOS）/主界面组：顶栏按钮样式 Tonal（默认）/合并顶栏按钮/大顶栏形态）
- 每轮 dump 节点完整、无缺失/重叠/截断；行高一致（约 216px 行距），标题行 150px 组距
- **判定：通过（观察）** —— 滚动流畅无卡顿痕迹（逐帧截图结构完整），行间距与分组观感稳定。

**证据**：vui_50_my.png、vui_51_settings.png、vui_52_appear_top.png、vui_53_appear_scrolled.png、vui_54_appear_bottom.png、vui_item7_my.xml / vui_item7_settings.xml / vui_item7_appear.xml / vui_item7_appear2.xml / vui_item7_appear3.xml

## 项8：崩溃面检查（logcat）

操作路径：`adb -s 127.0.0.1:16384 logcat -d`（main + crash 双 buffer）→ 按任务规范 grep `FATAL|AndroidRuntime.*legado`，并对 crash buffer 逐条核对归属进程（Cmdline 行）。

原始输出与判定：
- 规范 grep 命中 93 行 FATAL，逐条核对归属：全部为 `uiautomator`（Shutdown thread SIGSEGV，dump 退出时的自杀噪声，uid 2000 shell）与 `grep`（本机管道内 grep 进程）两类，Cmdline 非 uiautomator/grep 的崩溃记录为 **0**。
- `AndroidRuntime` 关键字命中 0 行；crash buffer 中 `legado` 关键字命中 0 行。
- 当前 io.legado.flutter_legado 进程 pid 4706 存活（pidof 确认），不在任何崩溃记录中（crash buffer 中出现的 "4706" 为时间戳数值巧合，已核对排除）。
- crash buffer 崩溃时间线 22:06–22:56 与本轮 uiautomator dump 调用时间点一一对应（每次 dump 退出即一条 uiautomator SIGSEGV），属已知 MuMu + uiautomator 组合噪声，与 app 无关。

**判定：通过** —— 复核窗口内无 legado 应用崩溃（FATAL/AndroidRuntime 均无 app 归属记录）。

证据：vui_item8_crashbuffer.log（crash buffer 全文 2062 行）、vui_item8_logcat_main.log（main buffer 全文）

---

# 差异点分类汇总

## 需修
（本轮无新增需修项。项1 的 D9 深色底色差异 #4A4A4A vs ref #101418 属主题引擎/色板取值，归入待裁决。）

## 待裁决
1. **项6 书架单击行为**：我方单击书架卡 → 书详页（再点「阅读」进入阅读）；参考基线行为为单击直达阅读（未读书 → 首章）。需 UI 批次确认：Flutter 化后是否刻意改为「详页中转」，或应恢复单击直达。证据 vui_44_shelf_tap_read.png / vui_item6_shelf.xml。
2. **项1 D9 深色底色**：我方 #4A4A4A（Material 中性深灰）vs 参考基线 #101418（偏蓝深）。若基线要求自定义色板「关闭（内置色板）」下的特定深底值，需确认色板映射是否遗漏。
3. **项4 阅读器菜单（文字书）**：无法在受限环境判定（见下），菜单键位是否对齐参考 5 键（全文搜索/自动翻页/目录/朗读/设置）待有可读文字书环境后补验。漫画读者菜单已取证（返回/漫画设置/进度/章节页码，vui_39_reader_menu.png）作替代证据，结构合理但未覆盖 5 键全集。

## 可忽略
- 项1 D6 分组命名差异（主题引擎/配色风格 vs 主题风格）：Flutter 重命名，语义等价。
- 项1 D7 标题形态差异（常规顶栏 vs large top app bar）：已知形态选择。
- 项1 D8 ref 中央预览图区我方无：ref 迁移期遗留。
- 项1 D10 ref「仍有部分界面未用 Compose 重构」提示条：ref 历史通知，我方全量 Flutter 后自然消失，非缺陷。
- 项1 D11 分辨率差异 1080x1920 vs 1080x2220：设备侧。
- 项5 失败书源横幅 111 个（受限外网，Network error/Timeout 为主）：环境网络问题，非 app 缺陷；横幅非阻断（结果列表在收起态 102px 横幅下方正常滚动，916/916 进度正常推进，favcomic/喜漫漫画在展开列表中可定位）。

# 环境受限无法判定项
1. **项4 阅读器菜单（文字书 5 键）**：受限外网下 111/916 书源失败，唯一可命中书源为漫画源 🏮Bi话（lbxjtjs.com/bh）且其目录解析失败 → 无可用文字书，文字阅读器菜单无法进入。已用漫画读者菜单作替代取证。
2. **项6 返回刷新新进度**：书架仅 1 本书且无阅读记录（阅读记录 tab 为空），无法构造「阅读 → 返回书架观察新进度副文案」闭环；书架卡副文案区（[66,902][342,1022] 单击热区下方）未观测到进度副文案节点，可能因该无阅读进度。

# 异常登记（应用内状态变更，未做破坏性操作）
1. 主题状态变更：复核中为验证选中态，将主题从「草野」切至「千禧年」又切回「草野」；深色模式/配色风格「关闭（内置色板）」/纯黑深色模式 OFF 等设置保持原值未改。当前残留：主题=草野、深色、纯黑 OFF。
2. 书架状态：全程保持 1 本书（斗破苍穹斗破苍穹 的搜索结果）。「移出书架」确认框曾误触发（双击第二击落在详页「已在书架」按钮），已点「取消」关闭，书未移出。
3. 搜索历史：书搜页点击历史项会追加/重复关键词（如「R1换源验证书R1换源验证书」），且输入框无法用硬件 DEL/全选程序化清空（Flutter EditText 对该 keyevent 不响应）→ 触发一次 916 书源重搜（进度 484/916 停留 60s 后放弃，按 2 次重试无进展规则跳过，BACK 回退书架）。属工具局限（Flutter 侧语义/键盘注入），非 app 缺陷。
4. 未执行任何应用内设置修改（项中涉及「如需改设置则记录」的情况未发生）。

# 证据清单
- 报告：VERIFY_UI_20260922.md（本文件）
- 截图（vui_ 前缀，56 张）：vui_00_home_initial.png … vui_54_appear_bottom.png（编号 00–54，含并排对比图 vui_05/06/08_theme_side 等）
- uiautomator dump XML（90 份）：vui_item2_*.xml / vui_item3_*.xml / vui_item4_*.xml / vui_item5_*.xml（含 vui_item5_banner_expanded.xml = 111 源失败全量列表）/ vui_item6_*.xml / vui_item7_*.xml，以及各页面辅助 dump（vui_home*.xml、vui_mine*.xml、vui_appear_*.xml、vui_settings_*.xml 等）
- 崩溃日志（2 份）：vui_item8_crashbuffer.log（crash buffer 全文）、vui_item8_logcat_main.log（main buffer 全文）
- 并排对比图由 /tmp/vui_tool.py side 子命令生成（左 参考基线 ref_dark_20260920 / 右 我方实机）

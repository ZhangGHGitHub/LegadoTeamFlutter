id: multi-agent-handoff-parallel-overhead-collapse-trigger
skill: multi-agent-handoff
routing: should_trigger
input: "A parallel subagent batch created coordination overhead but did not lower risk, uncertainty, or future repair time. Decide whether to leave native, reject the batch, collapse/delete scaffolding, and avoid a durable artifact."
rules:
  - kind: file_exists
    path: references/evals.md
  - kind: description_includes_any
    terms: ["multi-agent", "subagent", "terminal states"]
  - kind: body_includes_any
    path: SKILL.md
    terms: ["Cost/duplication", "leave_native", "rejected"]
  - kind: body_includes_any
    path: SKILL.md
    terms: ["Delete or collapse this batch section after synthesis"]
  - kind: body_includes_any
    path: SKILL.md
    terms: ["Do not preserve lane maps as project management state"]

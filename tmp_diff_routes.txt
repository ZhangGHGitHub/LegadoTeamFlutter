diff --git a/flutter_legado/lib/src/routes.dart b/flutter_legado/lib/src/routes.dart
index c7849dc2e..839234859 100644
--- a/flutter_legado/lib/src/routes.dart
+++ b/flutter_legado/lib/src/routes.dart
@@ -90,21 +90,36 @@ class AppRoutes {
         rss: (_) => const RssScreen(),
         audio: (context) {
           final args = ModalRoute.of(context)?.settings.arguments;
+          // 璺敱鍙傛暟瑙勮寖鍖栵細浼樺厛鎺ユ敹 Book 瀵硅薄
+          if (args is Book) {
+            return AudioScreen(book: args);
+          }
+          // 鍚戝悗鍏煎锛氭敮鎸?Map<String, String> 浼犲弬
           if (args is Map<String, String>) {
             return AudioScreen(
               bookUrl: args['bookUrl'] ?? '',
               bookName: args['bookName'] ?? '',
             );
           }
-          return const AudioScreen(bookUrl: '');
+          return const AudioScreen();
         },
         bookInfo: (context) {
           final args = ModalRoute.of(context)?.settings.arguments;
+          // 璺敱鍙傛暟瑙勮寖鍖栵細浼樺厛鎺ユ敹 Book 瀵硅薄
+          if (args is Book) {
+            return BookInfoScreen(book: args);
+          }
+          // 鍚戝悗鍏煎锛氭敮鎸?String(bookUrl) 浼犲弬
           final bookUrl = args is String ? args : '';
           return BookInfoScreen(bookUrl: bookUrl);
         },
         changeSource: (context) {
           final args = ModalRoute.of(context)?.settings.arguments;
+          // 璺敱鍙傛暟瑙勮寖鍖栵細浼樺厛鎺ユ敹 Book 瀵硅薄
+          if (args is Book) {
+            return ChangeSourceScreen(book: args);
+          }
+          // 鍚戝悗鍏煎锛氭敮鎸?Map<String, String> 浼犲弬
           if (args is Map<String, String>) {
             return ChangeSourceScreen(
               bookUrl: args['bookUrl'] ?? '',
@@ -113,12 +128,7 @@ class AppRoutes {
               currentSourceUrl: args['currentSourceUrl'] ?? '',
             );
           }
-          return const ChangeSourceScreen(
-            bookUrl: '',
-            bookName: '',
-            author: '',
-            currentSourceUrl: '',
-          );
+          return const ChangeSourceScreen();
         },
         readingStats: (_) => const ReadingStatsScreen(),
         bookmarks: (_) => const BookmarkScreen(),
@@ -141,19 +151,29 @@ class AppRoutes {
         bookGroups: (_) => const BookGroupScreen(),
         searchContent: (context) {
           final args = ModalRoute.of(context)?.settings.arguments;
+          // 璺敱鍙傛暟瑙勮寖鍖栵細浼樺厛鎺ユ敹 Book 瀵硅薄
+          if (args is Book) {
+            return SearchContentScreen(book: args);
+          }
+          // 鍚戝悗鍏煎锛氭敮鎸?Map<String, String> 浼犲弬
           if (args is Map<String, String>) {
             return SearchContentScreen(
               bookUrl: args['bookUrl'] ?? '',
               bookName: args['bookName'] ?? '',
             );
           }
-          return const SearchContentScreen(bookUrl: '', bookName: '');
+          return const SearchContentScreen();
         },
         about: (_) => const AboutScreen(),
         
         rssFavorites: (_) => const RssFavoritesScreen(),
         changeCover: (context) {
           final args = ModalRoute.of(context)?.settings.arguments;
+          // 璺敱鍙傛暟瑙勮寖鍖栵細浼樺厛鎺ユ敹 Book 瀵硅薄
+          if (args is Book) {
+            return ChangeCoverScreen(book: args);
+          }
+          // 鍚戝悗鍏煎锛氭敮鎸?Map<String, String> 浼犲弬
           if (args is Map<String, String>) {
             return ChangeCoverScreen(
               bookUrl: args['bookUrl'] ?? '',
@@ -161,7 +181,7 @@ class AppRoutes {
               currentCover: args['coverUrl'],
             );
           }
-          return const ChangeCoverScreen(bookUrl: '', bookName: '');
+          return const ChangeCoverScreen();
         },
         txtTocRules: (_) => const TxtTocRulesScreen(),
         dict: (_) => const DictScreen(),
diff --git a/flutter_legado/lib/src/screens/change_source_screen.dart b/flutter_legado/lib/src/screens/change_source_screen.dart
index 3357ea1f0..f43ca5eb2 100644
--- a/flutter_legado/lib/src/screens/change_source_screen.dart
+++ b/flutter_legado/lib/src/screens/change_source_screen.dart
@@ -3,6 +3,7 @@ import 'dart:convert';
 import 'package:flutter/material.dart';
 
 import '../bridge/rust_lib.dart' as bridge;
+import '../models/models.dart';
 import '../widgets/error_view.dart';
 import '../widgets/loading_indicator.dart';
 
@@ -63,6 +64,10 @@ class SourceMatchItem {
 /// `source_switch_apply`锛夛紝鍦ㄦ墍鏈夊惎鐢ㄧ殑涔︽簮涓悳绱㈠悓鍚嶄功绫嶏紝
 /// 鎸夊尮閰嶅害璇勫垎鎺掑簭锛岀敤鎴烽€夋嫨鍚庡垏鎹功绫嶆潵婧愩€? class ChangeSourceScreen extends StatefulWidget {
+  /// 涔︾睄瀵硅薄锛堣矾鐢卞弬鏁拌鑼冨寲锛氫紭鍏堜娇鐢?Book 瀵硅薄锛?+  final Book? book;
+
+  /// 浠ヤ笅瀛楁涓哄悜鍚庡吋瀹逛繚鐣欙紝褰撴湭浼犲叆 Book 瀵硅薄鏃朵娇鐢?   final String bookUrl;
   final String bookName;
   final String author;
@@ -70,12 +75,25 @@ class ChangeSourceScreen extends StatefulWidget {
 
   const ChangeSourceScreen({
     super.key,
-    required this.bookUrl,
-    required this.bookName,
-    required this.author,
-    required this.currentSourceUrl,
+    this.book,
+    this.bookUrl = '',
+    this.bookName = '',
+    this.author = '',
+    this.currentSourceUrl = '',
   });
 
+  /// 鑾峰彇鏈夋晥鐨?bookUrl
+  String get effectiveBookUrl => book?.bookUrl ?? bookUrl;
+
+  /// 鑾峰彇鏈夋晥鐨勪功鍚?+  String get effectiveBookName => book?.name ?? bookName;
+
+  /// 鑾峰彇鏈夋晥鐨勪綔鑰?+  String get effectiveAuthor => book?.author ?? author;
+
+  /// 鑾峰彇鏈夋晥鐨勫綋鍓嶄功婧愬湴鍧€
+  String get effectiveCurrentSourceUrl => book?.origin ?? currentSourceUrl;
+
   @override
   State<ChangeSourceScreen> createState() => _ChangeSourceScreenState();
 }
@@ -102,8 +120,8 @@ class _ChangeSourceScreenState extends State<ChangeSourceScreen> {
     });
     try {
       final jsonStr = await bridge.sourceSwitchSearch(
-        bookName: widget.bookName,
-        author: widget.author,
+        bookName: widget.effectiveBookName,
+        author: widget.effectiveAuthor,
       );
       final decoded = jsonDecode(jsonStr);
       final matches = <SourceMatchItem>[];
@@ -165,7 +183,7 @@ class _ChangeSourceScreenState extends State<ChangeSourceScreen> {
     setState(() => _applyingUrl = result.sourceUrl);
     try {
       final updatedJson = await bridge.sourceSwitchApply(
-        bookUrl: widget.bookUrl,
+        bookUrl: widget.effectiveBookUrl,
         newSourceUrl: result.sourceUrl,
         newBookUrl: result.bookUrl,
       );
@@ -198,7 +216,7 @@ class _ChangeSourceScreenState extends State<ChangeSourceScreen> {
   Widget build(BuildContext context) {
     return Scaffold(
       appBar: AppBar(
-        title: Text('鎹㈡簮 - ${widget.bookName}'),
+        title: Text('鎹㈡簮 - ${widget.effectiveBookName}'),
         actions: [
           IconButton(
             icon: const Icon(Icons.refresh),
@@ -270,7 +288,7 @@ class _ChangeSourceScreenState extends State<ChangeSourceScreen> {
 
   Widget _buildResultTile(BuildContext context, SourceMatchItem item) {
     final colorScheme = Theme.of(context).colorScheme;
-    final isCurrent = item.sourceUrl == widget.currentSourceUrl;
+    final isCurrent = item.sourceUrl == widget.effectiveCurrentSourceUrl;
     final isApplying = _applyingUrl == item.sourceUrl;
 
     return ListTile(
